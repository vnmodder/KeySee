export enum ColumnStyle{
    text,
    number,
    date,
    checkbox,
    dropdown,
    button,
}

export enum ControlStyle{
    input,
    number,
    dateTime,
    checkbox,
    radio,
    dropdown,
    button,
    file,
    label
}

export enum GroupStyle {
    normal,
    radio,
    custom
}